"""opsed — detect OP/ED segments in a local anime folder and write lossless chapters.

Public entry point: ``opsed.cli.main`` (also installed as the ``opsed`` command).
Every run operates on exactly one user-supplied folder; there is no default
library and no whole-library mode.
"""

__version__ = "0.1.0"
